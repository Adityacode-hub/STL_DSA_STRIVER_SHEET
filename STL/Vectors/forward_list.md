the forward list class is sequential container
 forward_list is based on singly linked list
 the header required is forward_list
 list provides forward iterator

 dsa:- singly linked list
  so the explicit iterator are:-
  ----------------------------------
  begin() end()
  cbegin() cend()
  before_begin()
  cbefore_begin()

------------------------------

method in  the forward_list

clear()

empty()

front()
 insert_after()

 emplace_after()

 erase_after()

 push_front()

 emplace_front()

 swap()
 merge()
 reverse()
 sort()



 there is only we can do is the 
 pop_front :-bcz no need to traverse


emplace_front() :-works better than the insert when u r working with the non premitive type data
