explixit iterators

begin() end()
rbegin()  rend()
cbegin() cend()

methods of the list class
------------------------------
back():- return the last elements
front():-returns the first elements
empty():- returns true or false
size():returns the number of the elements
swap():-swap the elements of two list l2.swap(l1)
clear() :- erase all the elements
merge():removes the elements from tje argument list,insert them into the target list and order the new combined set of elements in  the ascending order or in some other specific order (sorted order)

push_back()
pop_back()
push_front()
pop_front()
erase()
emplace()
emplace_back()
emplace_front()
insert()
reverse()
remove()

